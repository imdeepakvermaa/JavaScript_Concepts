function calculateCartPrice(...num1) {
  return num1;
}

// console.log(calculateCartPrice(200,4000,20000))

const worldcupinfo = {
  winningteam: "India",
  year: 2023,
  against: "Australia",
};

function worldCupFinal(anyobject) {
  console.log(
    `${anyobject.year} world cup winning team is ${anyobject.winningteam} against ${anyobject.against}`
  );
}

worldCupFinal(worldcupinfo)

